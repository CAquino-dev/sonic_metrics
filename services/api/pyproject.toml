[project]
name = "sonic-metrics-api"
version = "0.1.0"
description = "Backend API for Sonic Metrics"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[dependency-groups]
dev = [
    "pytest",
    "httpx",
]